[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "casting-check"
version = "0.1.0"
description = "Financial decimal casting and rounding validation tool"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
